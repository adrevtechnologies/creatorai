# Production environment template
# Copy to .env.production and fill real values (do not commit the real file)
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require&channel_binding=require
